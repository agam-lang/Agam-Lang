# Gemini CLI / Antigravity Context — Agam-Lang

> **Truth**: Read `note.md` before writing or documenting Agam code.
> **Verification**: Test all code via `python scripts/prove.py` or `agamc run` & `agamc build --backend llvm`.

## Quick Directives
- **Skills**: Auto-loaded from `.agent/skills/`. Highlights: `doctest-guard`, `diff-fuzz`, `unwrap-ratchet`, `audit-harden`, `cargo-lens`, `language-guard`, `safe-commit`.
- **Fast Build**: Use `python scripts/cargo_lens.py check` to save tokens.
- **Rules**: Inherited from `AGENTS.md` and `.agent/rules/`.
- **Active Phase**: Read `.agent/specs/active/current.md`.

